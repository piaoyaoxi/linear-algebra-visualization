# 第三章制作矩阵

> 内部制作与验收文件，不进入学生正文。
> 最后更新：2026-07-18

## 章节主线

```text
方程组
→ 增广矩阵与等价行变换
→ Fⁿ 中的未知向量
→ 线性组合、相关与极大无关组
→ 秩：独立信息的统一计数
→ rank(A) 与 rank([A|b]) 的可达性判别
→ 特解 + Ker(A) 的解族结构
→ 选学：多项式结式消元
```

## 制作原则

- 页面顺序统一为“概念问题 → 几何直觉 → 定理与结构 → 精确实验 → 例题与自测”。先建立可见对象，再引入形式化语言。
- 核心几何叙事采用主题自适应的内联 SVG；精确计算、RREF、完整坐标和参数验证收进默认折叠的精确实验。
- 不使用与参数脱节的装饰性位图。第三章的向量、子空间、网格变换和解族都要随步骤或参数实时变化，因此 SVG 比静态生成图像更合适。
- 数值线性代数统一使用精确有理数，不能以浮点阈值代替严格的秩、相关或有解判定。
- 低维 SVG/Canvas 只承担几何观察。高维判定必须使用完整坐标，投影必须明确标注。
- 每个视觉实验必须明确“当前看什么、为什么变化、最终读出什么结论”，不能让学生面对一堆控件自行猜测。
- 向量统一使用带明确箭头的有向位移；不得再用“线段加端点圆圈”冒充向量。
- 颜色承担稳定语义：主对象、第二方向、合成/目标对象在同一小节内保持一致，不为装饰随意换色。
- 例题先让学生选择，再展开完整分析；错误选项不能提前泄露答案。
- 学生页面不出现制作进度、占位模块或内部参考资料说明。
- 交互监听器在离开小节时全部清理；SVG 保持流式 viewBox，Canvas 按设备像素比绘制。

## 参考页索引

| 小节 | 精选参考 PDF 页码 | 主要视觉借鉴 |
| --- | ---: | --- |
| §1 消元法 | 1—13 | 共同交点不变、方程写法变化、主元与自由度 |
| §2 n维向量空间 | 14—25 | 坐标列、标准基、首尾相接的向量合成与投影边界 |
| §3 线性相关性 | 26—38 | 张成增长、非零关系证书、删除冗余 |
| §4 矩阵的秩 | 39—52 | 网格变换、面积/直线/点的维数塌缩、精确秩证书 |
| §5 有解判别 | 53—70 | 输入—输出空间、列空间可达性、目标向量与增广秩 |
| §6 解的结构 | 71—85 | 特解箭头、零空间方向、参数位移与仿射解集 |
| ＊§7 二元高次 | 86—92 | 曲线交点、坐标轴投影、结式候选与回代验证 |

## 全章视觉故事

| 小节 | SVG 推导步骤 | 精确实验保留内容 | 数学不变量 |
| --- | --- | --- | --- |
| §1 | 共同交点 → 可逆倍加 → 主元结构 | 三类行操作、阶梯形、RREF、撤销 | 行等价不改变解集 |
| §2 | 有向位移 → 缩放并接续 → 闭合成 w | n=1—8、完整坐标、分量条 | w=αu+βv 逐坐标同步 |
| §3 | 铺开 span → 加入 v₃ → 删除冗余 | 拖动、精确关系证书、删除测试 | rank 与零空间关系一致 |
| §4 | 保留面积 → 压成直线 → 压成一点 | RREF、主元列、非零子式、行操作 | rank 是像空间维数 |
| §5 | 先看 Col(A) → b 在线上 → b 在线外 | 增广矩阵、RREF、矛盾行 | rank(A)=rank([A|b]) iff solvable |
| §6 | 找到 x₀ → 看 Ker(A) → 生成全部解 | 特解切换、参数、双重验证 | Ax=b 且 A(x−x₀)=0 |
| ＊§7 | 看交点 → 投影 x 轴 → 形成结式 → 回代 | 相交/相切/无实点、x/y 消元 | 候选必须回代原方程 |

## 浏览器验收线

- 章节总览与 7 个小节路由均可进入。
- 每节的几何直觉位于定理与结构之前，精确实验默认折叠但可以完整打开操作。
- 桌面 1440×1000 与窄屏 390×844 无页面级横向溢出。
- 亮色、暗色与 prefers-reduced-motion 均可使用。
- 所有 KaTeX 已渲染，无生 LaTeX、无 `.katex-error`。
- 每个 SVG 步骤按钮、字幕、公式和图形状态同步；移动端步骤按钮使用两列完整网格，不出现残缺横向滚动。
- SVG 分组动画不得覆盖原有 transform 坐标；矩阵、标签、说明框必须处于设计位置。
- §2 的 αu、βv 与 w 必须能直接读出首尾相接关系，标签不得互相覆盖。
- §3 的 v₃ 必须同时有非零关系证书和“删除后 span 不变”的几何证据。
- §4 的网格必须明确呈现二维面积、一维直线和零维点三种输出。
- §5 的 b 必须以目标向量显示，而不是孤立点。
- §6 必须清晰区分 O→x₀、x₀→x 的零空间位移和 O→x，并显示所有解映到同一个 b。
- §7 候选点在完成回代前不得标为已验证解。
- 精确层继续使用完整维度和精确有理数，不得用低维 SVG 投影替代严格结论。
- 回归检查第四章成熟页面仍可正常打开。

## 2026-07-18 最终多轮验收

1. 第一轮浏览器检查发现 SVG 入场动画覆盖分组坐标，导致矩阵与标签挤到左上角；修复为只改变透明度，不再修改 SVG transform。
2. 第二轮检查覆盖 7 节 × 桌面、手机、暗色、减少动态效果，全部交互和回归检查通过；随后逐张查看截图。
3. 第三轮把几何故事移动到定理之前，并重新检查全章顺序、完整页面和手机端布局。
4. 最终轮将手机步骤改为两列网格，并增加按钮宽度与同排位置断言；7 节全部通过。

- 第二轮完整运行：`29639265231`。
- 几何优先顺序复核：`29639506831`。
- 最终移动端与全章复核：`29639703450`。
- 最终覆盖：桌面、移动端、亮暗主题、减少动态效果、全部步骤、参数滑块、精确实验展开、KaTeX、横向溢出、控制台错误与第四章回归。
