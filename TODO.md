# TODO

> **会话入口：** 每次开始工作前必须完整阅读本文件。这里保存项目硬规则、关键背景、当前状态和下一步；`docs/project-memory.md` 继续保存更完整的资料研究记录。

# 高等代数可视化项目记忆

> 内部项目文档，给 Codex / 后续新会话 / 上下文压缩后续接使用。
> 本文不是学生页面内容，不要直接展示在网站前台。
> 最后更新：2026-07-13

## 1. 项目定位

项目名称可以朴素地叫“高等代数可视化”或相近名称。核心定位是高等代数与线性代数的可视化伴学网站；章节标题沿用现有课程结构，具体内容优先综合经典美国教材。

网站的价值不在于替代教材，也不在于做题库或 AI 解题器。它要补足纸面教材和课堂板书很难表达的部分：空间如何变化、过程如何展开、抽象对象如何被看见、学生如何通过拖动参数得到即时反馈。

当前样板章是第四章“矩阵”。原因是矩阵连接了表格记号、运算、线性映射、复合、行列式、秩、可逆、初等变换和分块结构，适合先把网站形态打通。

## 2. 学生页面的硬规则

- 学生页面不显示“开发进度”“原型版本”“占位”“即将制作”“待补充”等项目内部语言。
- 学生页面没有真实视频源时，不显示视频区域。
- 学生页面没有真实交互内容时，不显示空交互区域。
- 避免在学生页面使用“不是……而是……”这类容易让学生误记的句式。用正向、直接的表达。
- 例题答案默认折叠或渐进展开，让学生先思考。
- 视觉效果只服务理解。玻璃质感、动效、首页氛围不能压过数学内容。
- 正文、公式、例题区要保持清晰实底；玻璃质感主要用于导航、侧栏、顶部控件和少量信息层。

## 3. 小节页面结构

推荐小节结构：

```text
起点问题
概念短讲视频：有真实视频源才显示
交互实验：有真实交互才显示
定理概念：KaTeX、教材对应位置、必要定义
代表例题：折叠或步进展开
自测：少量题，即时或折叠反馈
小结：一句主线、前后连接、常见误区、标记掌握
```

短片与交互的关系：

- 需要完整叙事的概念做短片，长度大约 1-3 分钟，可用 Manim，后续可接本地 TTS。
- 短片下面可以放交互实验，帮助学生把刚看到的动画改成自己可操作的实验。
- 只需要观察参数变化的小节，直接做交互实验。
- 偏符号规则、推导、教材地图性质的小节，保留导读、例题和自测即可。

## 4. 当前代码事实

当前主应用在 `current/`。

短片是可选的。`current/app.js` 里的 `getVideo(section)` 只在 `section.video.src` 或 `section.video.embed` 存在时返回视频。真实可播放内容继续放在 `video` 字段；未来短片脚本、镜头草案等内部计划放在 `videoPlan` 字段或文档里，不进入前台视频模块。

交互也是可选的。`getInteractive(section)` 先检查小节是否显式声明 `interactive`：有则按该字段渲染，`interactive: false` 可明确关闭；没有时再兼容读取 `visual`。自动识别的真实交互类型包括 `transform`、`multiply`、`rank`、`block`。`slot` 不自动出现，只用于显式 `interactive` 或后续增强脚本需要稳定容器的情况。

当前渲染顺序在 `renderLessonPage()` 中已经符合项目方向：

```text
概念问题 -> 视频 -> 交互 -> 定理概念 -> 代表例题 -> 自测 -> 小结
```

现有第四章关键资源：

- `transform`：矩阵改变网格，归入 §1。
- `multiply`：矩阵乘法三视角，归入 §2，是黄金样板核心交互。
- `rank`：秩与独立方向，归入 §3。
- `block`：已可通过 `interactive` 或 `visual.type = "block"` 渲染，归入 §5。
- `slot`：只作为显式增强容器，不自动视为真实交互动画。

## 5. 已看资料：线性代数不难

本地文件：

```text
/Users/lixiansheng/Desktop/线性代数不难(带书签) (生姜DrGinger) (z-library.sk, 1lib.sk, z-lib.sk).pdf
```

PDF 元信息：

- 作者元数据：James Jiang
- 页数：866
- A4，未加密
- 有 87 个书签

目录中适合我们借鉴的主题：

- 向量：向量加减法、数乘、内积、投影、范数。
- 矩阵：矩阵乘法、矩阵乘法的几何视角、矩阵乘法多个视角。
- 行列式：2x2、3x3、性质、手算。
- 逆矩阵：逆矩阵、性质、求逆。
- 向量空间、线性方程组、几何变换、投影、Gram-Schmidt、QR。
- 特征值分解、二次型、SVD 等后续可视化价值很高。

已经重点看过的小节：`2.5 矩阵乘法的几何视角`，PDF 物理页大约 190-202。

这一节的主线非常适合改编成动画：

```text
Ax = y
-> A 把输入向量从 p 维空间送到 m 维空间
-> 2x2 矩阵拖动平面网格
-> e1、e2 的去向决定矩阵两列
-> 旋转、缩放、镜像、剪切等例子
-> 3x3 空间变换
-> 3x2 把二维嵌入三维
-> 2x3 把三维投影到二维
-> 逆变换作为“把变化撤回去”
```

可借鉴点：

- 先从 `Ax=y` 的形状和输入输出讲起，再进入几何图像。
- 2x2 部分最适合做短片：两个基向量拖动整张网格。
- 3x2 / 2x3 很适合将来做“升维嵌入”和“降维投影”的交互。
- 这本书的图像思路很强，但网站应重画自己的动画和图，不直接复制图片或长段文字。

## 6. 已看资料：MIT 18.06

用户提供的样例链接：

- https://ocw.mit.edu/courses/18-06sc-linear-algebra-fall-2011/resources/mit18_06scf11_ses1-2sum/

课程资源入口：

- https://ocw.mit.edu/courses/18-06sc-linear-algebra-fall-2011/pages/resource-index/
- https://ocw.mit.edu/courses/18-06sc-linear-algebra-fall-2011/download/

当前判断：

- MIT 18.06SC 的资源页和课程下载页可以作为 PDF 讲义、习题、解答、考试等资料的索引入口。
- 它适合用来校对概念顺序、严谨表述、例题和习题，不适合作为主要视觉风格来源。
- 后续如果需要“列出所有 PDF”，单独做一次资源抓取和清单整理，不要混在页面开发里。

## 7. 已看资料：3Blue1Brown

用户提供的博客式课程页面：

- https://www.3blue1brown.com/lessons/vectors/

EOLA 代码库：

- https://github.com/3b1b/videos/tree/master/_2016/eola

已经下载到临时研究目录的代表源码：

```text
_dev/research/3b1b_eola_chapter3.py
_dev/research/3b1b_eola_chapter4.py
_dev/research/3b1b_eola_chapter10.py
```

重要代码观察：

- 3B1B 这套代码使用旧版 ManimGL / `manimlib` 风格。我们后续实现应优先用 ManimCE，并把思路翻译过去。
- `chapter3.py` 是线性变换核心：一个向量、很多向量、整张网格、基向量、矩阵列。
- 常见基类/方法：`LinearTransformationScene`、`VectorScene`、`apply_transposed_matrix(...)`。
- 叙事顺序值得借鉴：先动一个向量，再动很多向量，再动整张网格；最后记录 `i_hat`、`j_hat` 的落点，把它们拼成矩阵列。
- `chapter4.py` 是复合与矩阵乘法：先做旋转，再做剪切，最后把两次变换合成为一个乘积矩阵。
- `chapter10.py` 是特征值特征向量：`TweakLambda` 一类场景展示 `(A - lambda I)`、行列式变化、网格坍缩和特征方向。

最值得借鉴的是它的镜头语言：

```text
提出问题
-> 先给一个可见对象
-> 让对象连续变形
-> 保持颜色和对象连续性
-> 把图像落回公式
-> 用同一视觉语言解释下一层抽象
```

## 8. 第四章小节制作判断

第四章保留现有 §1—§7 小节标题作为内容边界；具体讲法、证明顺序与例题功能综合参考 Strang、Lay、Hoffman–Kunze、Friedberg、Axler 等美国教材，不再把中文教材正文作为内容锚点。

| 小节 | 内容方向 | 短片 | 交互/动画 | 备注 |
| --- | --- | --- | --- | --- |
| §1 矩阵概念背景 | 尺寸、下标、位置、按列阅读与列组合 | 暂不需要 | 基向量与网格、共线/零矩阵边界 | 长方形例题防止把矩阵误等同于二维几何。 |
| §2 矩阵的运算 | 先定义输出列，再推出行乘列与复合 | 保留 `videoPlan` | 连续复合、看列、行乘列、交换顺序 | 已从 `AB=[Ab₁ … Abₚ]` 建立完整因果链。 |
| §3 乘积的行列式与秩 | 交替多线性证明乘积公式；列空间与核空间证明秩上界 | 不需要独立短片 | 面积临界、乘积面积、秩瓶颈 | 低维画面只解释直觉，一般证明已进入正文。 |
| §4 矩阵的逆 | 双侧逆、唯一性、逆序、方程与计算选择 | 保留 `videoPlan` | 可逆往返与降秩断点 | 定义、证明、二阶求逆与直接消元的分工已闭环。 |
| §5 矩阵的分块 | 自然分组、合法尺寸、块乘法与结构零块 | 不需要 | 切割器、块乘法透镜、结构与转置 | 交互块尺寸与代表例题已统一。 |
| §6 初等矩阵 | 三类操作、左右乘、逆操作与增广求逆 | 不需要独立短片 | `I → E → EA`、左右乘、方程同步 | 已完成从单步操作到连续消元的证明链。 |
| §7 分块初等变换 | 一般块高斯消元、Schur 补、右端同步与回代 | 不需要 | 尺寸闸门、三类块操作、四步 Schur 消元 | 不再只讲特殊单位块系统。 |

## 9. 动画与交互的工程路线

短片路线：

```text
资料拆解
-> scenes.md 镜头脚本
-> ManimCE 代码
-> 渲染 mp4/webm
-> 可选 TTS 音频
-> 放入 current/assets/videos/
-> 在内容数据里填写 video.src 或 video.embed
```

交互路线：

```text
确定学生要通过操作回答的问题
-> 设计可调参数
-> 用 Canvas / SVG / HTML 控件实现
-> 放到 renderVisualPanel 或专门 presentation 脚本
-> 小节数据通过 visual / interactive 指定
```

重要原则：

- 短片负责讲清一条线性叙事。
- 交互负责让学生亲手改变参数，验证边界情况。
- 二者可以共用同一套数学对象和视觉语言。
- 没有操作问题的“交互”不要做；没有叙事必要的“短片”不要做。

## 10. 当前制作状态与下一步建议

已经完成：

1. `docs/ch4-teaching-blueprint.md` 与 `docs/ch4-production-matrix.md` 已按六本美国教材重新建立整章教学骨架。
2. §1—§7 的主问题、定义/定理、证明责任、教材思想来源、代表例题、自测、小结与练习均已重写。
3. 现有交互已按新正文重新对齐：§1 增加镜像边界，§3 补一般证明，§5 块尺寸与例题一致，§7 升级为一般 Schur 补消元。
4. `tests/ch4-content-audit.cjs` 会重新计算七道代表例题并检查来源、公式转义、单一内容源和呈现一致性；桌面、窄屏、明暗主题与七节交互均已通过浏览器检查。

接下来优先做这些：

1. 检查 PR #34 的独立预览并合并到 `main`；合并后删除任务分支。
2. 合并后从最新 `main` 开始下一章或下一项短片任务，不在本分支继续叠加无关内容。
3. 需要真实短片时，先把现有 `videoPlan` 转成 Manim 场景脚本；只有 mp4/webm 或 iframe 地址存在后才填写 `video`。

## 11. 未来追加记录格式

以后读到新资料时，按这个格式追加：

```markdown
### 日期 · 资料名 · 小节

- 来源：
- 对应教材章节：
- 这一节的主问题：
- 可改编成短片的镜头：
- 可改编成交互的操作：
- 可以进入例题/自测的点：
- 不适合照搬的地方：
- 后续动作：
```

## 12. Git 主干与并行开发规则

- 默认分支是 `main`，它保存当前已经验收的完整网站；仓库继续保持私有，`main` 本身不等于公开发布。
- 开发只保留内容与 UI 两条并行线。内容分支使用 `content/<task>`，UI 与交互分支使用 `ui/<task>`。
- 同一时间最多各有一条内容分支和 UI 分支。一个任务一个 PR，满意后尽快合并 `main` 并删除任务分支。
- 新任务必须从最新 `main` 创建。另一条线有 PR 合并后，尚未合并的分支应同步最新 `main`，再做组合页面验收。
- 不再长期堆叠 PR。确有依赖时最多短暂两层；前置 PR 合并后立即把后续 PR 迁回 `main`。
- `current/learn.html`、`current/app.js` 和共享 registry/mount 文件是集成热点，不让两个 Agent 同时无协调修改。
- `gh-pages` 只负责公开 Pages，不作为开发基线，也不直接编辑。`main` 每次更新后，部署工作流会把 `current/` 同步到 Pages 固定根地址；每个 PR 仍发布到独立的 `previews/pr-<编号>/` 地址。两个工作流都会在并发推送冲突时自动 fetch、rebase 并重试，首页同步会保留所有 PR 预览目录。

## 当前状态

- 主应用在 `current/`。
- 第四章保留现有 §1—§7 标题；标题决定内容范围，正文综合 Strang、Lay、Hoffman–Kunze、Friedberg、Axler 与 LALFD，不再依赖中文教材正文。
- 七节详细内容分别位于 `current/content/ch4-section1.js`、`ch4-section2.js`、`ch4-section3.js`、`ch4-section4.js`、`ch4-core.js`、`ch4-block.js`；`ch4.js` 只保留章节元数据和小节索引。
- §3 已把体积动画与一般证明分开：行列式乘积用交替多线性唯一性证明，两个秩上界分别用列空间包含与核空间包含证明。
- §5 的块乘法工作台与代表例题共用 2+1 的内部块尺寸；§7 使用一般块系统并由块高斯消元推出 Schur 补、变换后右端和回代公式。
- KaTeX 已本地化到 `current/vendor/katex/`。
- 已完成的基础能力：公式渲染、内容分离、进度联动、搜索恢复、路由滚动、第四章总览卡片。
- 短片字段已校准：真实可播放内容使用 `video`，未来短片脚本使用 `videoPlan`。
- 交互字段已校准：需要稳定交互容器的小节使用 `interactive`，`block` 可正常渲染，`slot` 只作为显式增强容器。
- `docs/ch4-teaching-blueprint.md` 记录教材分工、整章主线、证明责任与验收标准；`docs/ch4-production-matrix.md` 记录逐节制作映射。
- `tests/ch4-content-audit.cjs` 已通过：七节结构、美国教材来源、TeX 转义、例题唯一答案及全部矩阵计算均已核验。
- 浏览器验收已通过：七节桌面与 390px 窄屏无根级横向溢出，KaTeX 与控制台无错误；代表例题作答后题面继续可见，解析只在作答后展开。
- 原 PR #1—#5、#7—#22 的 stacked PR 链已在 2026-07-13 整理并合并；默认分支已统一为 `main`。
- 旧功能分支已经清理；常态只保留 `main` 与部署分支 `gh-pages`，有任务时再增加一条内容分支和一条 UI 分支。
- PR 预览工作流已支持并行推送重试，避免不同 PR 同时写 `gh-pages` 时发生 non-fast-forward 失败。
- Pages 固定根地址自动跟随已验收的 `main`；不提交 PR 时也可以从固定首页查看当前主干网站，PR 页面继续用于查看尚未合并的改动。
- 第四章 §1—§7 已接入统一例题反馈：先选择、错误重试、正确后展开解析，并播放 π 粒子反馈。
- 统一例题组件位于 `current/visuals/example-challenge.js` 与 `current/visuals/example-challenge.css`；没有选择项的例题自动使用逐步展开模式。
- 例题公式继续使用本地 KaTeX；手机端粒子数量已降低，并支持 `prefers-reduced-motion`。

## 第二章状态（2026-07-17）

- 已从 main 建立 `content/ch2-determinants`。
- 课程引擎支持多章结构化小节（ch2 + ch4）。
- 第二章 §1—§8 内容、例题、自测与核心交互已接入 `learn.html`。
- 精选参考 PDF 仅作内部研究，不进入公开站点路径。

## 下一步

- 内容线：提交并推送本次第四章七节教材重构，更新现有 PR，目标分支保持 `main`。
- 合并前不要在 `current/learn.html` 或第四章共享呈现文件上开启另一项并行修改。
- 下一轮概念短片制作从现有 §2/§4 `videoPlan` 选择一条主线，不显示没有真实媒体源的视频模块。
