name: Apply Chapter 10 mobile cinematic header fix

on:
  push:
    branches: [content/ch10-dual-bilinear-symplectic]
    paths: [scripts/ch10-mobile-safe-header.patch.gz.b64]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: content/ch10-dual-bilinear-symplectic
          fetch-depth: 0
      - name: Apply safe-header patch
        run: |
          set -euo pipefail
          base64 -d scripts/ch10-mobile-safe-header.patch.gz.b64 | gzip -d > /tmp/ch10-mobile-safe-header.patch
          git apply --check /tmp/ch10-mobile-safe-header.patch
          git apply /tmp/ch10-mobile-safe-header.patch
          rm scripts/ch10-mobile-safe-header.patch.gz.b64
          rm .github/workflows/ch10-apply-mobile-safe-header.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(ch10): keep mobile cinematic titles below floating chrome"
          git push origin HEAD:content/ch10-dual-bilinear-symplectic