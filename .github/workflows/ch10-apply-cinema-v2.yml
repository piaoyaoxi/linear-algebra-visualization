name: Apply Chapter 10 cinematic refinement

on:
  push:
    branches: [content/ch10-dual-bilinear-symplectic]
    paths: [scripts/ch10-cinema-v2.patch.gz.b64]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: content/ch10-dual-bilinear-symplectic
          fetch-depth: 0
      - name: Apply verified refinement patch
        run: |
          set -euo pipefail
          base64 -d scripts/ch10-cinema-v2.patch.gz.b64 | gzip -d > /tmp/ch10-cinema-v2.patch
          git apply --check /tmp/ch10-cinema-v2.patch
          git apply /tmp/ch10-cinema-v2.patch
          rm scripts/ch10-cinema-v2.patch.gz.b64
          rm .github/workflows/ch10-apply-cinema-v2.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor(ch10): refine cinematic mobile layouts"
          git push origin HEAD:content/ch10-dual-bilinear-symplectic