# 第四章矩阵可视化网站：最终制作蓝图

> 用途：供 Agent、开发者、动画设计与内容校对共同执行。  
> 参考资料：《线性代数不难》PDF（内部研究参考），参考页面已按本章七节重排为 `chapter4-visual-reference-pages.pdf`。  
> 原则：吸收数学叙事、视觉问题与信息组织方式；不得直接复制原书图片、构图、配色或动画。网站中的图形、交互、例题表达与视觉资产必须重新设计。

---

## 0. 交付物与文件对应关系

- 本文档：`chapter4-visualization-production-blueprint.md`
- 参考页 PDF：`chapter4-visual-reference-pages.pdf`
- 原始参考书：`线性代数不难.pdf`

参考 PDF 按以下区间组织：

| 小节 | 参考 PDF 页码 | 原书物理页 |
|---|---:|---|
| §1 矩阵概念的一些背景 | 1-51 | 143-150、168-177、191-202、345-353、424-430、434-437 |
| §2 矩阵的运算 | 52-134 | 156-164、179-186、191-214、218-225、240-246、251-256、439-445、462-465、467-475 |
| §3 矩阵乘积的行列式与秩 | 135-198 | 228-236、270-276、281-286、289-296、345-353、439-445、467-475、483-490 |
| §4 矩阵的逆 | 199-297 | 298-309、311-329、389-407、409-417、439-445、452-460、467-475、483-490、493-498 |
| §5 矩阵的分块 | 298-355 | 156-164、168-177、218-225、228-236、240-246、251-256、260-267 |
| §6 初等矩阵 | 356-400 | 289-296、384-388、409-417、439-445、467-475、493-498 |
| §7 分块乘法的初等变换及应用举例 | 401-418 | 260-267、409-417 |

> “原书物理页”指整个 PDF 从第一页开始计数的实际页序，不是页面页脚中每章重新编号的 Page 1、Page 2。

---

## 1. 全章统一制作规则

### 1.1 每节页面骨架

每一节原则上按以下顺序组织；没有必要的视频模块可以省略，但其余模块应保持一致的阅读节奏：

1. 小节标题、核心问题、学习目标与关键词。
2. 概念问题：先提出为什么，而不是先堆定义。
3. 直觉引入：视频、自动动画或静态对照三选一。
4. 核心讲解：定义、规则、性质及前后连接。
5. 可视化实验：每个交互必须回答一个明确问题。
6. 代表例题：题目默认显示，答案折叠。
7. 自测：3-8 道短题，根据本节内容密度决定。
8. 小结、常见误区和下一节衔接。

### 1.2 视频、动画与交互的分工

- **讲解视频**：用于存在清晰叙事顺序、多个视角需要统一的概念。视频时长控制在 1.5-3 分钟。
- **自动动画**：用于步骤固定、无需学生调参的内容，例如转置翻折、撤销倒序、行列配对。
- **可视化交互**：用于参数边界、临界状态和反例，例如两列共线、行列式变零、块尺寸不匹配。
- **例题**：把视觉直觉落回符号计算，不用海量刷题。

### 1.3 交互统一规则

- 参数改变后，公式、矩阵、图形和结论必须同步更新。
- 颜色语义固定：同一列向量、基向量、网格方向和公式项使用一致颜色。
- 特殊状态应提供预设按钮，不要求学生手动凑出单位矩阵、共线、投影等关键情况。
- 交互离开或重置后恢复稳定初始状态。
- 不能只提供“好看”的拖动；页面必须提出观察任务或问题。
- 支持键盘操作、可见焦点和 `prefers-reduced-motion`。
- 移动端可降低交互复杂度，但不得丢失核心解释。

### 1.4 例题统一规则

- 题目默认可见，解析默认折叠。
- 选择题始终允许重新选择。
- 错误选项轻微晃动并给出方向性提示，不直接揭晓答案。
- 正确后再展开分步解析；每一步只推进一个逻辑动作。
- 正确反馈可使用短暂 π 粒子，颜色服从网站主色。
- 解析必须同时包含“计算”和“为什么”。

### 1.5 版权与视觉改造规则

- 参考 PDF 仅供内部研究与动画构思，不随公开网站或公共仓库发布。
- 不直接截取原书图片放入网站。
- 不逐像素重画原书构图。
- 允许借鉴：数学对象、问题顺序、变量关系、连续变化机制、对比结构。
- 必须重做：配色、布局、图形风格、标注系统、动效节奏、交互控件、例题文案。

---

# §1 矩阵概念的一些背景

## 参考资料

- 参考 PDF：第 1-51 页。
- 原书物理页：143-150、168-177、191-202、345-353、424-430、434-437。
- 主要用途：矩阵行列结构、行列拆分、常见形状、输入输出维数、列向量与网格、线性无关的预备直觉。

## 核心问题

> 矩阵为什么会出现？它为什么不只是一张数字表格？

## 学习目标

学生完成本节后应能：

- 正确读出矩阵的行、列、阶与元素位置。
- 解释下标 `a_ij` 的先行后列规则。
- 判断两个矩阵何时相等。
- 区分行向量、列向量、方阵、长方形矩阵、零矩阵和单位矩阵。
- 解释 `m × n` 矩阵的输入维数与输出维数。
- 在二维中从矩阵的两列读出两个标准基向量的去向。

## 页面讲解顺序

### 1. 开场：同一组数字为什么会变成矩阵

用三种场景并排呈现：

- 数据表：行表示对象，列表示属性。
- 方程组：抽取系数，形成统一记录。
- 线性变换：各列记录基本方向经过变换后的坐标。

三种场景最后收拢为同一个矩阵。核心句：矩阵不是数字集合，而是带位置与关系的结构。

**媒介**：20-30 秒自动动画，不做独立视频。

### 2. 交互：Matrix Anatomy（矩阵解剖）

显示一个一般的 `3 × 4` 矩阵。

功能：

- 悬停或点击元素，高亮所在行和列。
- 显示对应符号 `a_ij`。
- 切换“按行拆开”“按列拆开”“查看元素”。
- 点击第 `j` 列时，显示其列向量形式。

必须讲清：

- `m × n` 是 `m` 行、`n` 列。
- `a_ij` 的第一个下标是行，第二个下标是列。
- 矩阵的行、列和元素位置是结构的一部分。

### 3. 小交互：矩阵相等

给出四组判断：

- `2 × 3` 与 `3 × 2`，元素总数相同但形状不同。
- 形状相同但一个位置不同。
- 矩阵与它的转置。
- 两个真正逐项相同的矩阵。

结论：形状相同且对应位置全部相同，才是矩阵相等。

### 4. 基础矩阵角色

只介绍后续反复出现的对象：

- 行向量、列向量。
- 方阵与长方形矩阵。
- 零矩阵：所有输出落到零。
- 单位矩阵：什么也不改变。

对称矩阵、三角矩阵、正交矩阵等不在本节展开。

### 5. 交互：Shape Machine（尺寸机器）

页面结构：输入端口 → `m × n` 矩阵 → 输出端口。

学生调节 `m,n`：

- 左侧出现 `n` 个输入坐标。
- 右侧出现 `m` 个输出坐标。
- 矩阵行列同步改变。

预设：`2 × 2`、`3 × 2`、`2 × 3`。

### 6. 主交互：Basis-to-Grid（两列决定网格）

以二维矩阵为中心，同时显示：

- 第一列及 `Ae1`。
- 第二列及 `Ae2`。
- 单位正方形和完整网格的变化。

学生拖动四个元素或直接拖动两列向量。

预设：

- 单位矩阵。
- 横向拉伸。
- 剪切。
- 两列共线。
- 零矩阵。

引导任务：

1. 先确认单位矩阵。
2. 只改变第一列，观察一组网格线。
3. 只改变第二列，观察另一组网格线。
4. 让两列共线，观察平面为何坍缩。

## 代表例题

矩阵 `[[2,1],[0,1]]`：

- 读出两个基向量的像。
- 判断网格是怎样拉伸和剪切的。
- 判断是否压扁平面。

分步解析：读第一列 → 读第二列 → 判断是否共线 → 描述网格 → 得出不坍缩。

## 自测范围

- 矩阵阶数。
- 元素位置。
- 矩阵相等。
- 输入输出维数。
- 第 `j` 列的含义。
- 两列非零是否一定满秩。

## 验收标准

- 学生不看定义也能通过交互指出 `a_23` 的位置。
- 切换行/列视角时信息不会混乱。
- 调整尺寸时输入输出端口正确。
- 两列共线时网格明确坍缩，不出现数值不稳定或残影。
- 本节不加入与标题无关的特殊矩阵大全。

---

# §2 矩阵的运算

## 参考资料

- 参考 PDF：第 52-134 页。
- 原书物理页：156-164、179-186、191-214、218-225、240-246、251-256、439-445、462-465、467-475。
- 主要用途：转置翻折、乘法尺寸、行乘列、几何复合、结合律与不可交换、四种乘法视角、缩放/旋转/剪切过程。

## 核心问题

> 矩阵为什么能相加、数乘和相乘？矩阵乘法为什么表示两个过程的复合？

## 学习目标

- 掌握加法、数乘和转置。
- 掌握乘法尺寸条件与行乘列公式。
- 用复合、看列、行乘列三种主视角理解同一个乘积。
- 理解单位矩阵、结合律和通常不交换。
- 初步理解左乘更自然地改行、右乘更自然地改列。

## 页面讲解顺序

### 1. 起点问题

先展示“逐项乘法”和真正矩阵乘法不同，提出：若矩阵记录过程，两个过程连续发生应该怎样合成？

### 2. 加法与数乘

- 同型矩阵逐项相加。
- 数乘作用于所有元素。
- 尺寸不一致时显示无法对齐的边框。

**媒介**：短动画，不做大型交互。

### 3. Transpose Flip（转置翻折）

沿主对角线翻折：

- 行变列。
- `a_ij` 移到 `a_ji`。
- 细高矩阵变扁平。
- 对称矩阵翻折后重合。

必须包含：

- 双重转置。
- 加法、数乘的转置。
- 乘积转置倒序。

### 4. 尺寸闸门

输入 `A_(m×n)` 与 `B_(r×p)`：

- `n=r` 时闸门打开。
- 否则拒绝乘法。
- 结果尺寸由外侧 `m,p` 决定。

### 5. Row × Column Lens（行乘列透镜）

布局：`A` 在左、`B` 在上、`C=AB` 在右下。

点击 `c_ij`：

- 高亮 `A` 的第 `i` 行。
- 高亮 `B` 的第 `j` 列。
- 对应项依次相乘并求和。

### 6. 讲解视频：矩阵乘法为什么是复合

**时长**：2-3 分钟。

镜头顺序：

1. `x` 先经过 `B`。
2. 中间向量保持颜色，继续经过 `A`。
3. 两次网格变化合并为 `AB`。
4. 强调靠近 `x` 的矩阵先作用。
5. 切到“看列”。
6. 切到“行乘列”。
7. 三种视角统一。

### 7. Three Views of Multiplication（三视角交互）

同一组矩阵、同一组颜色、同一组输入，切换：

- 复合：`ABx=A(Bx)`。
- 看列：`AB` 的第 `j` 列是 `A` 作用于 `B` 第 `j` 列。
- 行乘列：`c_ij` 的计算。

### 8. 单位矩阵

`IA=A`、`AI=A`。使用“穿过机器但不改变”的动画。

### 9. 结合律与交换律

- 结合律：实际机器顺序不变，只改变打包方式。
- 不可交换：横向拉伸与剪切的两种顺序并排播放。
- 逻辑澄清：不满足交换律不等于任何一对矩阵都不能交换。

### 10. 左乘与右乘预告

显示 `EA` 与 `AE`：

- 左乘突出行组合。
- 右乘突出列组合。

这里只建立直觉，正式规则放到 §6。

### 11. 可选扩展

- 矩阵幂：重复施加同一变换。
- 非零矩阵零乘积：`B` 的输出落入 `A` 消灭的方向。
- 特殊可交换矩阵：同方向剪切或对角矩阵。
- 邻接矩阵幂计数路径：只作为折叠应用，不进入主考核。

## 代表例题

### 例题 1：`AB` 与 `BA`

取横向拉伸矩阵与剪切矩阵：

- 检查尺寸。
- 分别计算 `AB`、`BA`。
- 用动作顺序解释右上角元素差异。

### 例题 2：尺寸与行乘列

给 `2 × 3` 与 `3 × 2` 矩阵，要求写出结果尺寸并只计算一个指定元素。

## 自测范围

- 加法条件。
- 转置后的形状。
- 乘法尺寸。
- 作用顺序。
- 行乘列。
- 乘积的列解释。
- 结合律和交换律。
- 左乘与右乘。

## 验收标准

- 三视角切换时，矩阵和图形必须保持同一组数据。
- `AB` 与 `BA` 动画必须明确显示顺序而非只给结果。
- 转置动画不允许只交换块位置而忽略元素内部位置。
- 视频缺少真实文件时，不显示空视频模块。

---

# §3 矩阵乘积的行列式与秩

## 参考资料

- 参考 PDF：第 135-198 页。
- 原书物理页：228-236、270-276、281-286、289-296、345-353、439-445、467-475、483-490。
- 主要用途：外积和秩一、二维/三维行列式、乘积行列式、线性无关与冗余、缩放/剪切/投影造成的维数变化。

## 核心问题

> 连续作用后，面积倍率怎样变化？真正保留下来的独立方向有多少？

## 学习目标

- 理解行列式的有向面积意义。
- 理解乘积行列式。
- 理解秩 2、1、0。
- 理解行秩、列秩与转置。
- 理解乘积秩上界和“方向不可恢复”。
- 理解可逆因子保持秩。

## 页面讲解顺序

### 1. 讲解视频：倍率相乘，方向只能保持或丢失

**时长**：约 2 分钟。

- 单位正方形先经 `B`、再经 `A`。
- 面积倍率依次相乘。
- 另一条镜头中 `B` 把平面压成线。
- 后续 `A` 只能改变这条线，无法恢复平面。

### 2. 有向面积

- 正行列式：方向保持。
- 负行列式：方向翻转。
- 零行列式：坍缩。

顶点顺序必须可视化，不能只写“有向面积”。

### 3. Determinant-Rank Lab（统一实验室）

学生拖动两列，页面同步显示：

- 两个列向量。
- 平行四边形。
- 行列式数值与符号。
- 秩。
- 可逆状态。

预设：单位、拉伸、剪切、镜像、投影、共线、零矩阵。

必须表现：

- 行列式连续趋近 0。
- 秩只在临界状态发生离散跳变。

### 4. `det(A)=1` 不代表没有变形

并排展示单位矩阵、面积保持的非等比例缩放、剪切。三者面积倍率相同但形状不同。

### 5. 乘积行列式

面积计量器：面积 1 → 乘 `det(B)` → 再乘 `det(A)`。

对照 `AB` 与 `BA`：形状通常不同，但总面积倍率相同。

### 6. 整体数乘

用二维和三维模式解释 `det(kA)=k^n det(A)`。只需滑条和一道题。

### 7. 秩的三联画

- 秩 2：平面。
- 秩 1：直线。
- 秩 0：点。

### 8. 删除冗余向量

给三列，其中一列是另两列组合。学生删除冗余列，观察张成空间与秩不变。

### 9. 行秩与列秩

- 定义行秩、列秩。
- 点击转置，行列交换但秩不变。
- 证明放折叠区，主页面只建立结构。

### 10. Rank Bottleneck（秩瓶颈）

固定秩 1 的 `B`，切换可逆 `A`：旋转、剪切、缩放、镜像。输出仍为一条线。

切换不可逆 `A` 时，秩可能继续下降。

### 11. 可逆因子保持秩

明确给出并解释：

- `A` 可逆时 `rank(AB)=rank(B)`。
- `B` 可逆时 `rank(AB)=rank(A)`。

### 12. 可选扩展

- 外积生成秩一矩阵。
- `Im(B)` 落入 `ker(A)` 解释零乘积。
- 三维体积只作扩展，不占主交互。

## 代表例题

1. `B` 的两列相同，判断 `AB` 两列关系与秩上界。
2. 上三角矩阵乘积：用对角元计算行列式，改变一个对角元为 0 后判断秩与可逆性。

## 自测范围

- 行列式正负与零。
- `det(AB)`。
- `det(kA)`。
- 秩 2/1/0。
- 行秩列秩。
- 乘积秩上界。
- 可逆因子保持秩。

## 验收标准

- 面积、行列式、秩、可逆性四项必须同步。
- 共线临界点不能误报秩。
- `AB`/`BA` 对照应展示“形状不同、面积相同”。
- 不引入 SVD、特征值或大篇幅拉普拉斯展开。

---

# §4 矩阵的逆

## 参考资料

- 参考 PDF：第 199-297 页。
- 原书物理页：298-309、311-329、389-407、409-417、439-445、452-460、467-475、483-490、493-498。
- 主要用途：余子式与伴随矩阵、逆变换、不可逆投影、方程解的几何、初等行变换求逆、缩放/旋转/剪切/镜像的逆。

## 核心问题

> 什么时候输出可以唯一还原为输入？不可逆究竟丢失了什么？

## 学习目标

- 掌握逆矩阵定义。
- 理解逆是撤销操作。
- 连接可逆、非零行列式、满秩、线性无关、唯一解和不坍缩。
- 掌握乘积求逆倒序。
- 掌握二阶公式、伴随矩阵法和初等行变换三种路线。

## 页面讲解顺序

### 1. 讲解视频：可逆就是信息没有丢失

**时长**：1.5-2 分钟。

- 网格经过 `A` 变形，再经 `A^-1` 恢复。
- 缩放、旋转、剪切可撤销。
- 投影让多个输入重合，无法倒推。

### 2. Undo Transformation（撤销动画）

时间轴：原图 → `A` → 变形图 → `A^-1` → 原图。

预设：缩放、旋转、剪切、镜像。

### 3. 不可逆多对一实验

选择两个不同输入点，投影后重合。提出“只知道输出能否找回原输入”。

### 4. Invertibility Dashboard（可逆性总控面板）

同一矩阵同步显示：

- 两列是否独立。
- 行列式。
- 秩。
- 网格是否坍缩。
- `Ax=b` 是否对每个 `b` 唯一。
- 是否存在逆。

### 5. 乘积求逆

原过程先 `B` 后 `A`；撤销先 `A^-1` 后 `B^-1`。

允许学生选择错误撤销顺序并看到无法复原。

### 6. 逆矩阵的行列式

用面积倍率解释 `det(A^-1)=1/det(A)`。

### 7. 特殊矩阵的逆

折叠卡片：

- 对角矩阵。
- 旋转矩阵。
- 镜像矩阵。
- 单位矩阵。

### 8. 非方阵边界

解释普通双侧逆为何主要针对方阵。左逆、右逆、伪逆不展开。

### 9. 三种求逆路线

#### 二阶公式

检查行列式 → 交换主对角元素 → 非主对角变号 → 除以行列式。

#### Cofactor Builder（余子式拼装器）

点击元素位置 → 删除对应行列 → 余子式 → 代数余子式 → 转置 → 伴随矩阵 → 除以行列式。

完整演示一个简单 `3 × 3` 或 `2 × 2` 例子。

#### 初等行变换

`[A|I] → [I|A^-1]`，本节只讲结果，机制留到 §6。

## 代表例题

1. 二阶矩阵求逆并检查乘积为单位矩阵。
2. 两列成比例的矩阵，从列关系、行列式和网格三方面判断不可逆。
3. 简单伴随矩阵法例题。

## 自测范围

- 逆矩阵定义。
- 撤销倒序。
- 可逆等价条件。
- 行列式与逆。
- 特殊矩阵的逆。
- 二阶公式。
- 伴随矩阵步骤。

## 验收标准

- 总控面板中所有判定必须互相一致。
- 不可逆案例必须明确表现多对一，而非只显示“det=0”。
- 余子式拼装器必须突出删行删列和最后转置。
- 不进入伪逆、SVD 或正交矩阵完整理论。

---

# §5 矩阵的分块

## 参考资料

- 参考 PDF：第 298-355 页。
- 原书物理页：156-164、168-177、218-225、228-236、240-246、251-256、260-267。
- 主要用途：分块、转置、四种乘法视角、完整块乘法。

## 核心问题

> 为什么按有意义的行列分组后，大矩阵的结构和计算会变清楚？

## 学习目标

- 理解合法分块。
- 掌握分块加法、数乘、转置和乘法。
- 把矩阵乘法四种视角统一为不同切法。
- 理解块对角和块上三角。

## 页面讲解顺序

### 1. Block Cutter（分块切割器）

拖动完整行/列切割线，生成不同块。每块显示尺寸。

强调：分块不改变原矩阵，只重新组织结构。

### 2. 分块加法与数乘

两个矩阵切法不兼容时拒绝逐块相加；学生调整切割线后解锁。

### 3. 分块转置

两步动画：

1. 大块交换位置。
2. 每个块内部转置。

### 4. Block Multiplication Lens（块乘法透镜）

点击 `C_ij`：

- 高亮 `A` 第 `i` 块行。
- 高亮 `B` 第 `j` 块列。
- 显示块尺寸。
- 逐项块乘并求和。

### 5. Four Block Views（四种切法）

- 按行/列切到标量元素：行乘列。
- 按列/行切：外积之和。
- 只切 `B`：结果列。
- 只切 `A`：结果行。

### 6. 块对角结构

两个并行画布展示互不耦合子系统。补充矩阵幂和逆可按块处理。

### 7. 块上三角结构

用箭头表现单向依赖，为 §7 块消元铺垫。

## 代表例题

写出 `2 × 2` 分块乘积的右上块，完整说明取哪一块行、哪一块列及尺寸条件。

## 自测范围

- 合法分块。
- 分块加法。
- 分块转置。
- 指定结果块。
- 块对角与块上三角。

## 验收标准

- 切割线只能沿完整行列移动。
- 所有块显示尺寸，错误乘法不可执行。
- 四种切法必须使用同一乘积数据。
- 不加入一般块逆、块行列式、Kronecker 积或 Schur 补专题。

---

# §6 初等矩阵

## 参考资料

- 参考 PDF：第 356-400 页。
- 原书物理页：289-296、384-388、409-417、439-445、467-475、493-498。
- 主要用途：行列式性质、特殊矩阵与行操作、初等行变换、缩放/剪切/镜像的几何直觉。

## 核心问题

> 为什么一次行变换等于左乘一个矩阵？为什么每个初等操作都可以撤销？

## 学习目标

- 掌握三类初等行变换。
- 会从行操作构造初等矩阵。
- 理解左乘改行、右乘改列。
- 理解初等矩阵可逆和行等价。
- 理解行列式、秩、主元和求逆之间的联系。

## 页面讲解顺序

### 1. 三类操作

- 换行。
- 非零倍乘。
- 倍加。

解释为什么倍乘系数不能为 0。

### 2. 几何辅助直觉

- 交换对应坐标置换/镜像。
- 倍乘对应缩放。
- 倍加对应剪切。

### 3. I → E → EA Stepper

三栏同步：

1. 对单位矩阵做操作。
2. 得到初等矩阵 `E`。
3. 左乘 `EA`，在任意 `A` 上执行相同行操作。

默认案例：`R2 ← R2 - 3R1`。

### 4. 左乘与右乘

同一个 `E`：

- `EA` 高亮行变化。
- `AE` 高亮列变化。

交换和倍加各演示一次。

### 5. 逆操作

- 换行的逆仍是换行。
- 乘 `c` 的逆是乘 `1/c`。
- 加 `c` 倍的逆是加 `-c` 倍。

### 6. Equation-Matrix-Geometry Sync

同步展示方程组、增广矩阵和直线交点。行变换后表达式改变，但解集保持。

### 7. 行列式与秩状态面板

- 换行：行列式变号，秩不变。
- 倍乘：行列式乘系数，秩不变。
- 倍加：行列式不变，秩不变。

### 8. 行等价与连续左乘

显示 `B=E_k...E_1A`，逐步累积当前初等矩阵。

### 9. 高斯消元、主元与秩

消元到阶梯形，显示主元个数、非零行数与秩一致。

### 10. 求逆闭环

若 `E_k...E_1A=I`，则初等矩阵乘积就是 `A^-1`；展示 `[A|I]→[I|A^-1]`。

## 代表例题

1. 从 `R1 ← R1 + 2R2` 写出 `E` 和 `E^-1`。
2. 给交换矩阵，区分 `EA` 与 `AE`。
3. 从三步消元过程写出初等矩阵乘积。

## 自测范围

- 三类操作。
- 构造/识别初等矩阵。
- 左乘右乘。
- 逆操作。
- 行列式和秩变化。
- 行等价。
- 消元求逆。

## 验收标准

- 步进器每一步只改变指定行/列。
- 单位矩阵与原矩阵必须同步执行同一操作。
- 方程几何视图的交点在可逆行变换后保持。
- 不做海量高斯消元刷题或 LU 分解。

---

# §7 分块乘法的初等变换及应用举例

## 参考资料

- 参考 PDF：第 401-418 页。
- 原书物理页：260-267、409-417。
- 主要用途：将原书的分块乘法与普通初等行变换组合成原创块消元叙事。

## 核心问题

> 怎样把普通行操作升级成块行操作，并用它消去子系统之间的耦合？

## 学习目标

- 理解标量倍数升级为矩阵块。
- 掌握三类块初等变换。
- 会构造分块初等矩阵。
- 检查块尺寸合法性。
- 理解块消元和按块回代。

## 页面讲解顺序

### 1. 标量到矩阵块的过渡动画

`R2 ← R2 - cR1` 中的标量格子逐渐扩展为矩阵块，得到 `R2 ← R2 - CR1`。

### 2. 尺寸检查交互

学生选择 `C` 的尺寸。只有 `CR1` 与 `R2` 块结构一致时才允许操作。

### 3. 三类块初等变换

- 交换块行。
- 一个块行左乘可逆矩阵。
- 一个块行加另一块行的矩阵倍。

### 4. 分块初等矩阵

对分块单位矩阵做相同操作。例如 `[[I,0],[-C,I]]`。

### 5. Block Elimination Stepper（块消元步进器）

从一般 `2 × 2` 分块矩阵出发：

1. 选择要消去的块。
2. 检查必要的可逆条件。
3. 构造块初等矩阵。
4. 左乘。
5. 目标块抵消。
6. 得到块上三角结构。

### 6. 耦合到单向依赖

用两个子系统和箭头显示：完全耦合 → 消掉一个耦合块 → 块上三角 → 先解一个块、再回代。

## 代表例题

系统 `x=f`、`Cx+y=g`：

- 写成分块方程。
- 执行 `R2 ← R2 - CR1`。
- 得到 `x=f`、`y=g-Cf`。

附带验证：`[[I,0],[-C,I]][[I,0],[C,I]]=I`。

## 自测范围

- 块尺寸。
- 三类块操作。
- 构造分块初等矩阵。
- 左乘改变块行。
- 消去目标块。
- 按块回代。

## 验收标准

- 任何块操作都必须在界面上显示尺寸依据。
- 抵消动画必须明确显示 `C` 与 `-C` 的来源。
- 例题必须把矩阵变化与方程右端同步更新。
- 不正式展开一般 Schur 补、通用块逆或块行列式。

---

# 2. 全章视频配置

| 小节 | 视频 | 主题 |
|---|---|---|
| §1 | 不需要 | 动画和交互足够 |
| §2 | 需要 | 矩阵乘法为什么表示复合 |
| §3 | 建议需要 | 面积倍率相乘，方向丢失不可恢复 |
| §4 | 建议需要 | 可逆就是信息没有丢失 |
| §5 | 不需要 | 分块依靠直接操作 |
| §6 | 不需要 | 步进器优于被动视频 |
| §7 | 不需要 | §5 与 §6 的操作性合流 |

---

# 3. 核心组件清单

1. Matrix Anatomy
2. Shape Machine
3. Basis-to-Grid
4. Transpose Flip
5. Row × Column Lens
6. Three Views of Multiplication
7. AB vs BA
8. Determinant-Rank Lab
9. Rank Bottleneck
10. Undo Transformation
11. Invertibility Dashboard
12. Cofactor Builder
13. Block Cutter
14. Block Multiplication Lens
15. Four Block Views
16. I → E → EA Stepper
17. Equation-Matrix-Geometry Sync
18. Block Elimination Stepper

---

# 4. Agent 建议执行顺序

## 阶段 1：共用视觉基础设施

- 统一二维坐标系、网格、向量、矩阵输入、颜色语义。
- 建立可复用的播放/暂停/重置/预设控制。
- 建立统一的公式与图形同步更新机制。

## 阶段 2：§1 基础组件

1. Matrix Anatomy。
2. Shape Machine。
3. Basis-to-Grid。

验收后再进入乘法，避免后续重复造网格与矩阵控件。

## 阶段 3：§2 样板节

1. Transpose Flip。
2. Row × Column Lens。
3. Three Views of Multiplication。
4. AB vs BA。
5. 例题和自测。

§2 作为全章视觉与交互样板。

## 阶段 4：§3 与 §4 核心结构

1. Determinant-Rank Lab。
2. Rank Bottleneck。
3. Undo Transformation。
4. Invertibility Dashboard。
5. Cofactor Builder。

## 阶段 5：§6 初等矩阵

先做 I → E → EA Stepper，再做方程/矩阵/几何同步。§6 稳定后才能做 §7。

## 阶段 6：§5 与 §7

1. Block Cutter。
2. Block Multiplication Lens。
3. Four Block Views。
4. Block Elimination Stepper。

## 阶段 7：视频与最终内容收口

- 制作 §2、§3、§4 视频。
- 核对每节正文、例题、自测和小结。
- 删除无真实视频源的占位模块。
- 完成移动端、键盘操作和 reduced-motion 检查。

---

# 5. 每个 Agent 任务的提交格式

每次只处理一个明确组件或一个小节中的一个闭环，提交说明必须包含：

- 解决的教学问题。
- 新增/修改文件。
- 交互参数和预设。
- 错误状态、边界状态。
- 桌面端与移动端行为。
- 验收步骤。
- 对应参考 PDF 页码。

不得一次性重写七节；不得在没有验收的情况下把所有可选增强同时实现。

---

# 6. 最终完成定义

第四章完成并不是“所有模块都能显示”，而是满足：

- 七节标题和内容严格对应。
- 每节只有一个清晰主问题。
- 公式、图形、动画和例题互相解释，而非重复堆叠。
- 所有交互都能回答一个明确问题。
- 视频只在真正需要时出现。
- 例题默认显示、解析折叠、反馈一致。
- 参考书素材已完成明显原创改造。
- 全章在桌面、移动端和 reduced-motion 环境下可用。
