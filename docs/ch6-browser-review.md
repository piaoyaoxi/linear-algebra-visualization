# 第六章浏览器截图复核

本文件记录 PR #39 当前版本的截图复核结论。截图由 `Chapter 6 browser check` 工作流在真实 Chromium 中生成。

## 覆盖范围

- 桌面亮色：8 节
- 桌面暗色：8 节
- 手机亮色：8 节
- 手机减少动态效果：8 节
- 合计：32 张完整页面截图

## 逐节复核

| 小节 | 正式内容 | 交互实验 | 复核结论 |
|---|---|---|---|
| §1 集合·映射 | 映射构成、像与原像、单满双、复合与逆按顺序展开 | 左右集合和箭头是主视觉，右侧只保留四道判定与原像读取 | 不再出现旧仪表盘堆叠；学生能沿箭头完成固定检查顺序 |
| §2 线性空间定义 | 对象实例、公理分组、简单性质推导、反例策略完整 | 每个候选只展示一个关键反例；RGB 为三通道 | 二维与三通道反例含义清楚，结论不依赖猜测颜色或指标 |
| §3 维数·基与坐标 | 张成、无关、基、维数、坐标和提取基形成连续叙事 | 背景表示张成范围，箭头表示生成元；可删除冗余 | “范围有没有扩大”与“是否冗余”可以直接从图上读取 |
| §4 基变换与坐标变换 | 同一向量双基图、过渡矩阵推导、主动/被动对照、退化条件 | 两种模式完全分开；被动看固定端点，主动看两个箭头 | 原截图中的公式逐字竖排已消失；页面成为本章核心样板 |
| §5 线性子空间 | 定义、统一判定、原点筛查、标准来源和仿射平移完整 | 原点、红色反例与三道判定一致 | 学生能区分“形状像平面”和“具有线性结构” |
| §6 子空间的交与和 | 交、和、集合并、维数公式、基与例题均有独立模块 | R²/R³/R⁴ 使用不同但一致的视觉语言 | 公共方向与重复计数关系清楚，维数账本不再只是数字仪表 |
| §7 子空间的直和 | 覆盖、零交、唯一性证明、非唯一公式和正交特例完整 | 四种关键组合直接对照 | “存在”和“唯一”两道闸门可独立识别 |
| §8 线性空间同构 | 线性、双射、坐标同构、维数分类及结构层级完整 | 两条运算路径并列，单射/满射分别验收 | 同构失败原因在对应闸门中明确显示 |

## 版式复核

- 桌面端内容区保持单一阅读主轴，正式内容与实验之间有明确层级。
- 暗色主题中边框、文字、网格和状态色均保持足够区分。
- 手机端所有多栏结构按语义顺序改为单栏，不存在页面级横向溢出。
- 长公式位于独立公式容器；行内公式保持单行，不再被窄卡片逐字符折断。
- 每一节交互的“观察任务”已经与实际控件逐项核对，不再出现要求学生操作页面上不存在的参数。
- 第四章第一节回归正常。

## 最新复核

- 内容任务与实际按钮、滑块、状态再次逐项对齐后，浏览器工作流重新通过。
- PR 预览部署重新通过。
- 最终代码状态保持八节独立 renderer，不再加载任何旧通用第六章 renderer。
